{
  "name": "poolai-e2e",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "test:ci": "playwright test smoke admin a11y visual jobs_raid jobs_lease jobs_migrating protocol_middleware telegram_wallet grid_pricing grid_job_lease grid_result_lease",
    "test:headed": "playwright test --headed",
    "test:visual": "playwright test visual",
    "test:visual:update": "playwright test visual --update-snapshots"
  },
  "devDependencies": {
    "@axe-core/playwright": "4.10.1",
    "@playwright/test": "1.49.1"
  }
}
