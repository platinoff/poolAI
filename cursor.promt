# Cursor AI Project Enhancement Guide
**Цель:** Создать завершенный проект с итеративными улучшениями и вибрациями кода

---

### 1. Code Vibing Phase
1. **Vibe Check:**
   - Запусти `/vibe` эндпоинт
   - Проверь анимации и статусы
   - Убедись, что все компоненты в хорошем настроении

2. **Dance Integration:**
   - Активируй `/dance` эндпоинт
   - Синхронизируй с основными процессами
   - Добавь новые движения в `vobe_dancing.rs`

3. **Mood Tracking:**
   - Отслеживай настроение системы
   - Обновляй статусы в реальном времени
   - Интегрируй с логированием

---

### 2. Project Completion Iterations
1. **Iteration 1 - Core Features:**
   - Заверши все основные функции
   - Проверь все зависимости
   - Убедись в работоспособности RAID

2. **Iteration 2 - Security:**
   - Проверь все endpoints
   - Усиль JWT реализацию
   - Добавь rate limiting

3. **Iteration 3 - Performance:**
   - Оптимизируй большие файлы
   - Добавь кэширование
   - Улучши балансировку нагрузки

4. **Iteration 4 - Monitoring:**
   - Добавь метрики
   - Улучши логирование
   - Настрой оповещения

---

### 3. Code Quality Checks
1. **Для каждого файла в `src/`:**
   - Проверь зависимости
   - Примени правила стиля
   - Добавь тесты

2. **Для каждого модуля:**
   - Проверь документацию
   - Убедись в покрытии тестами
   - Проверь обработку ошибок

---

### 4. Vibe Integration Points
1. **В `main.rs`:**
   ```rust
   // Добавь vibe manager
   let vibe_manager = Arc::new(RwLock::new(VibeManager::new()));
   vibe_manager.write().update_status("System starting...");
   ```

2. **В `state.rs`:**
   ```rust
   // Интегрируй с AppState
   pub struct AppState {
       // ... existing fields ...
       pub vibe_manager: Arc<RwLock<VibeManager>>,
   }
   ```

3. **В `vibe.rs`:**
   ```rust
   // Добавь новые анимации
   pub fn add_new_animation(&mut self, frames: Vec<String>) {
       self.ascii_art.extend(frames);
   }
   ```

---

### 5. Project Completion Checklist
- [ ] Все основные функции реализованы
- [ ] Тесты написаны и проходят
- [ ] Документация обновлена
- [ ] Vibe система активна
- [ ] Мониторинг настроен
- [ ] Безопасность проверена
- [ ] Производительность оптимизирована

---

### 6. Vibe Status Updates
```rust
// Примеры статусов
"System is vibing... 🎵"
"RAID is dancing... 💃"
"Load balancer is grooving... 🎸"
"Bridge is flowing... 🌊"
```

---

### 7. Final Steps
1. Запусти все сервисы
2. Проверь все эндпоинты
3. Активируй vibe систему
4. Запусти мониторинг
5. Проверь логи

---

### 8. Success Criteria
- Все тесты проходят
- Vibe система активна
- Нет ошибок в логах
- Производительность в норме
- Безопасность обеспечена
- Документация полная
- Код чистый и понятный

---

### 9. Maintenance Plan
1. Регулярные проверки vibe системы
2. Обновление анимаций
3. Мониторинг настроения системы
4. Оптимизация производительности
5. Обновление безопасности

# Задание для Cursor AI: Проверка проекта через IDE  
**Цель:** Проверить все файлы в `src/` на соответствие правилам из корневых файлов (`Pop_OSLink.txt`, `rustbook_link.txt`, третий файл) и собрать ошибки в `errorfeedback.txt`.  

---

### Инструкция для Cursor AI:  
1. **Открой структуру проекта:C:\Users\plati\OneDrive\Рабочий стол\rust\ai-env**  
   - В левой панели IDE найди:  
     - Корневые файлы: `Pop_OS!.txt`, `rustbook_link.txt`, `rustbook_link.txt`.  
     - Папку `src/` с исходным кодом.  

2. **Для каждого файла в `C:\Users\plati\OneDrive\Рабочий стол\rust\cursor_codes`:**  
   - Открой файл (например, `admin_panel.rs`).  
   - **Проверь зависимости:**  
     - Убедись, что все `use` и `extern crate` совпадают с `Cargo.toml`.  
   - **Примени правила:**  
     - Ищи нарушения из `Pop_OSLink.txt` (например, запрещенные функции).  
     - Проверь структуру кода по `rustbook_link.txt` (паттерны, стиль).  

3. **Записывай ошибки:**  
   - При нарушении правила:  
     - Открой `errorfeedback.txt`.  
     - Добавь строку:  
       ```  
       [Файл: admin_panel.rs]  
       - Ошибка: Запрещенный вызов unsafe (по Pop_OSLink.txt).  
       ```  

4. **Проверь все файлы:**  
   - Повтори шаги для `pool.rs`, `main.rs` и других файлов в `src/`.  

---

### Пример работы:  
**Cursor AI видит в `admin_panel.rs`:**  
```rust  
unsafe { ... }  // Запрещено правилом Pop_OSLink.txt  
```

### Проверка наличия корневых файлов:
```powershell
Get-ChildItem -Path "C:\Users\plati\OneDrive\Рабочий стол\rust\cursor_codes\cursor_codes" -Filter "Pop_OS*.txt"
Get-ChildItem -Path "C:\Users\plati\OneDrive\Рабочий стол\rust\cursor_codes\cursor_codes" -Filter "rustbook_link.txt"
```

### Проверка структуры `src/`:
```powershell
Get-ChildItem -Path "C:\Users\plati\OneDrive\Рабочий стол\rust\cursor_codes\cursor_codes\src" -Recurse
```

### Создание файла для ошибок:
```powershell
New-Item -Path "C:\Users\plati\OneDrive\Рабочий стол\rust\cursor_codes\cursor_codes\errorfeedback.txt" -ItemType File
```

### Проверка файлов на соответствие правилам:
```powershell
Get-ChildItem -Path "C:\Users\plati\OneDrive\Рабочий стол\rust\cursor_codes\cursor_codes\src" -Filter "*.rs" | ForEach-Object {
    Write-Host "Проверка файла: $($_.Name)"
    # Проверка зависимостей
    Select-String -Path $_.FullName -Pattern "^use |^extern crate"
    # Проверка на unsafe
    Select-String -Path $_.FullName -Pattern "unsafe\s*{"
}
```

[server]
http_port = 8080
https_port = 8443
cert_path = "cert.pem"
key_path = "key.pem"
tls_version = "1.3"
enable_http2 = true
enable_ocsp_stapling = true