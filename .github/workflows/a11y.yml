# FM-019: pa11y WCAG 2.2 scan (18 strict URLs). See docs/development/ADMIN_A11Y_RUNBOOK.md §3.
# Invoked from ci.yml when UI paths change; manual: workflow_dispatch.
name: A11y (pa11y)

on:
  workflow_call:
  workflow_dispatch: {}

env:
  CARGO_TERM_COLOR: always
  K8S_OPENAPI_ENABLED_VERSION: "1.28"
  POOLAI_HTTP_PORT: "8080"
  PA11Y_ADMIN_STRICT: "1"
  PA11Y_WCAG22: "1"
  PA11Y_USER: admin
  PA11Y_PASSWORD: admin123

jobs:
  pa11y:
    name: Pa11y WCAG 2.2 (login + 21 auth URLs)
    runs-on: ubuntu-latest
    timeout-minutes: 45

    steps:
      - uses: actions/checkout@v6

      - uses: dtolnay/rust-toolchain@stable

      - uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Cache cargo
        uses: actions/cache@v5
        with:
          path: |
            ~/.cargo/registry/index/
            ~/.cargo/registry/cache/
            ~/.cargo/git/db/
            target/
          key: ${{ runner.os }}-a11y-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-a11y-

      - name: Build poolai (enterprise)
        run: cargo build --release --features enterprise,ml,cloud,test-utils

      - name: Run pa11y (WCAG 2.2 axe + login + 21 auth URLs)
        run: bash bin/pa11y-ci.sh --start
