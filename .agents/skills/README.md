# PoolAI skills

**Shared VDT kit (abracadabra host, marketplace architecture/TDD/debug, …)** lives in
`S:\rust\GSV\.agents\skills\`. Edit there, then `bash scripts/sync-vdt-skill-mirrors.sh` in GSV.

This tree keeps:

- `poolai-documentation` — PoolAI docs map (steps 1–12, FM, DIGEST)
- `abracadabra` — **fallback** router if this folder is the only open workspace; canon is GSV

Do not treat marketplace copies here as git-canon after GSV band 127.
